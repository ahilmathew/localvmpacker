#cloud-config
autoinstall:
  version: 1
  locale: en_US
  keyboard:
    layout: us
  network:
    network:
      version: 2
      ethernets:
        enp0s3:
          dhcp4: true
  identity:
    hostname: ubuntu-vm
    username: ahilmathew
    password: "$6$rounds=4096$c3LcaBr8DbWrzbB5$o\/5bgSna9hCTiI\/.OJ51llRjaYuGkpD8ITYz2yz1KXjiwe9V147gzRRIIhLVCf9mMNpB7Pd0kyJUpykv14GpD\/"
  early-commands:
    # otherwise packer tries to connect and exceed max attempts:
    - systemctl stop ssh
  ssh:
    install-server: true
    allow-pw: true
    authorized-keys: []
  late-commands:
    - sed -i -e 's/^#\?PasswordAuthentication.*/PasswordAuthentication yes/g' /target/etc/ssh/sshd_config
    - echo 'ahilmathew ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/ahilmathew
    - curtin in-target --target=/target -- chmod 440 /etc/sudoers.d/ahilmathew
  storage:
    layout:
      name: direct
# ssh_pwauth: true
# write_files:
#   - path: /etc/sudoers.d/90-cloud-init-users
#     content: |
#       ahilmathew ALL=(ALL) NOPASSWD:ALL
#     permissions: '0440'  
runcmd:
  - echo "Example command" > /root/example.txt
  - sed -i 's/^#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
  - systemctl restart ssh